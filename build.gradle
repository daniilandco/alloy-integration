subprojects {
    group = 'com.github.daniilandco'
    version = '0.0.1-SNAPSHOT'

    repositories {
        mavenCentral()
    }
}

task build {
    dependsOn ':api:build'
    dependsOn ':server:build'
}

task run {
    dependsOn ':server:bootRun'
}